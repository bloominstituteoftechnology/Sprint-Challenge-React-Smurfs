import React from 'react';

import Smurf from './smurf';

const SmurfDetails = props =>{
    // const smurfs = props.smurfs.filter(item => {return item.id === props.match.params.id; {
    //  return (<Smurf smurfs={item}/>)
    // });
        
}

export default SmurfDetails;